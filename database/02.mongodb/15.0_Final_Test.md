#  MongoDB Final Exam

**Total Questions:** 150
**Duration:** 180 Minutes (3 Hours)
**Sections:** A (MCQs) & B (Practical Queries)
**Instructions:** Answer all questions. For queries, write the exact MongoDB command.

---

#  SECTION A – MCQs (Q1–Q100)
*Dataset-Based – Intermediate Level*

##  PART 1 – JOBS DATASET (Q1–Q60)

**Database:** `codinggita`  
**Collection:** `jobs`

### Structure
```json
{
  "company": "String",
  "role": "String",
  "experience": "Number",
  "salaryLPA": "Number",
  "location": "String",
  "jobType": "String",
  "skills": ["String"]
}
```

### Q1. Which query finds jobs where salaryLPA is greater than 30?
A) `{ salaryLPA: { $gte: 30 } }`  
B) `{ salaryLPA: { $gt: 30 } }`  
C) `{ salaryLPA: 30 }`  
D) `{ salaryLPA > 30 }`

### Q2. Which query finds jobs where experience is exactly 3?
A) `{ experience: { $eq: 3 } }`  
B) `{ experience: 3 }`  
C) Both A and B  
D) `{ experience == 3 }`

### Q3. Find jobs where company is Google AND location is Bangalore.
A) `{ company:"Google", location:"Bangalore" }`  
B) `{ $and:[ {company:"Google"}, {location:"Bangalore"} ] }`  
C) Both A and B  
D) `{ company:"Google" || location:"Bangalore" }`

### Q4. Which query finds jobs where salaryLPA is between 20 and 30 inclusive?
A) `{ salaryLPA:{ $gt:20,$lt:30 } }`  
B) `{ salaryLPA:{ $gte:20,$lte:30 } }`  
C) `{ salaryLPA:[20,30] }`  
D) `{ salaryLPA:{ $range:[20,30] } }`

### Q5. Which query finds jobs where experience is NOT equal to 2?
A) `{ experience:{ $ne:2 } }`  
B) `{ experience:{ $not:2 } }`  
C) `{ experience !=2 }`  
D) `{ experience:{ $exclude:2 } }`

### Q6. Which query ensures both "MongoDB" and "Node.js" exist in skills?
A) `{ skills:{ $in:["MongoDB","Node.js"] } }`  
B) `{ skills:{ $all:["MongoDB","Node.js"] } }`  
C) `{ skills:["MongoDB","Node.js"] }`  
D) `{ skills:{ $size:2 } }`

### Q7. Which query checks experience is even?
A) `{ experience:{ $mod:[2,0] } }`  
B) `{ experience:{ $even:true } }`  
C) `{ experience%2==0 }`  
D) Not possible

### Q8. Which query compares salaryLPA and experience?
A) `{ salaryLPA:{ $gt:experience } }`  
B) `{ $expr:{ $gt:["$salaryLPA","$experience"] } }`  
C) `{ $compare:["salaryLPA","experience"] }`  
D) Not possible

### Q9. Projection to show only company and salaryLPA without _id:
A) `{ company:1, salaryLPA:1 }`  
B) `{ company:1, salaryLPA:1, _id:0 }`  
C) `{ company:true, salaryLPA:true }`  
D) `{ company:1, salaryLPA:0 }`

### Q10. Correct skip value for page=3, size=10:
A) 20  
B) 30  
C) 10  
D) 40

### Q11. Which index best supports filtering by location and sorting by salaryLPA descending?
A) `{ salaryLPA:-1 }`  
B) `{ location:1, salaryLPA:-1 }`  
C) `{ salaryLPA:-1, location:1 }`  
D) `{ location:1 }`

### Q12. If explain() shows COLLSCAN, it means:
A) Index used  
B) Full collection scanned  
C) Join performed  
D) Error

### Q13. Which stage groups jobs by company?
A) `$match`  
B) `$group`  
C) `$project`  
D) `$sort`

### Q14. To count total jobs per company:
A) `$group` with `$sum:1`  
B) `$count`  
C) Both A and B  
D) `$size`

### Q15. Which pipeline is correct to find average salary per location?
A) `$group` only  
B) `$match` → `$group`  
C) `$group` → `$match`  
D) `$sort` → `$group`

### Q16. Which query finds jobs where salaryLPA is less than or equal to 20?
A) `{ salaryLPA: { $lt: 20 } }`  
B) `{ salaryLPA: { $lte: 20 } }`  
C) `{ salaryLPA: 20 }`  
D) `{ salaryLPA: { $le: 20 } }`

### Q17. Which query finds jobs where location is NOT "Remote"?
A) `{ location: { $ne: "Remote" } }`  
B) `{ location: { $not: "Remote" } }`  
C) `{ location != "Remote" }`  
D) `{ location: { $exclude: "Remote" } }`

### Q18. Which query finds jobs where experience is either 2, 4, or 6?
A) `{ experience: [2,4,6] }`  
B) `{ experience: { $in: [2,4,6] } }`  
C) `{ experience: { $all: [2,4,6] } }`  
D) `{ experience: { $or: [2,4,6] } }`

### Q19. Which query correctly finds jobs where company is neither Google nor Amazon?
A) `{ company: { $ne: ["Google","Amazon"] } }`  
B) `{ company: { $nin: ["Google","Amazon"] } }`  
C) `{ $nor: [{company:"Google"}, {company:"Amazon"}] }`  
D) Both B and C

### Q20. Which query finds jobs where salaryLPA is greater than 25 AND experience is at least 3?
A) `{ salaryLPA:{ $gt:25 }, experience:{ $gte:3 } }`  
B) `{ $and:[{salaryLPA:{ $gt:25 }},{experience:{ $gte:3 }}] }`  
C) Both A and B  
D) `{ $or:[...] }`

### Q21. Which projection hides only the skills field?
A) `{ skills:0 }`  
B) `{ skills:false }`  
C) `{ skills:null }`  
D) `{ skills:remove }`

### Q22. Which projection shows only company, role, and location?
A) `{ company:1, role:1, location:1 }`  
B) `{ company:true, role:true, location:true }`  
C) Both A and B (excluding `_id` by default)  
D) `{ company:1, role:1, location:1, _id:0 }`

### Q23. If index is { location:1, salaryLPA:-1 }, which query uses prefix rule correctly?
A) Filter by location only  
B) Filter by salary only  
C) Sort by salary only  
D) Filter by experience

### Q24. If query filters only by salaryLPA but index is { location:1, salaryLPA:1 }, what happens?
A) Full index used  
B) Partial index used  
C) Prefix rule violated  
D) Error

### Q25. Which aggregation finds total jobs per location?
A) `$group: { _id:"$location", total:{ $sum:1 } }`  
B) `$match: { location }`  
C) `$project: { location }`  
D) `$sort: { location:1 }`

### Q26. Which aggregation calculates average salary per company?
A) `$group: { _id:"$company", avgSalary:{ $avg:"$salaryLPA" } }`  
B) `$group: { company:{ $avg:"$salaryLPA" } }`  
C) `$avg: "$salaryLPA"`  
D) `$project: { avgSalary }`

### Q27. Which pipeline order is correct for performance?
A) `$group` → `$match`  
B) `$match` → `$group`  
C) `$sort` → `$match`  
D) `$limit` → `$match`

### Q28. Which stage calculates new field doubleSalary = salaryLPA * 2?
A) `$project`  
B) `$addFields`  
C) `$group`  
D) `$match`

### Q29. Which query finds jobs where salaryLPA is greater than experience × 6?
A) `$expr` with `$multiply`  
B) `$gt` only  
C) `$sum`  
D) Not possible

### Q30. Which stage sorts jobs by salaryLPA descending?
A) `{ $sort: { salaryLPA: -1 } }`  
B) `{ $sort: { salaryLPA: 1 } }`  
C) `{ $order: { salaryLPA: -1 } }`  
D) `{ $arrange: { salaryLPA: -1 } }`

### Q31. To get top 5 highest salary jobs using aggregation:
A) `$limit:5` only  
B) `$sort` → `$limit`  
C) `$limit` → `$sort`  
D) `$group`

### Q32. Which explain output stage indicates index usage?
A) `COLLSCAN`  
B) `IXSCAN`  
C) `SORT`  
D) `LIMIT`

### Q33. If docsExamined = 1000 and nReturned = 10, performance is:
A) Efficient  
B) Poorly optimized  
C) Perfect  
D) Indexed

### Q34. Which index type is best for searching skills using keywords?
A) Compound  
B) Hashed  
C) Text  
D) Multikey

### Q35. Hashed index is best suited for:
A) Range queries  
B) Sorting  
C) Equality queries  
D) Regex

### Q36. Which query finds jobs where salaryLPA is NOT less than 20?
A) `{ salaryLPA:{ $gte:20 } }`  
B) `{ salaryLPA:{ $not:{ $lt:20 } } }`  
C) Both A and B  
D) `{ salaryLPA:20 }`

### Q37. Which query finds jobs where experience is less than 3 OR salaryLPA ≥ 30?
A) `$or` query  
B) Implicit AND  
C) `$all`  
D) `$size`

### Q38. Which query checks that skills array contains at least one element?
A) `{ skills:{ $exists:true } }`  
B) `{ skills:{ $size:1 } }`  
C) `{ skills:{ $not:{ $size:0 } } }`  
D) `{ skills:true }`

### Q39. Which stage renames salaryLPA to salary in output?
A) `$project:{ salary:"$salaryLPA" }`  
B) `$group`  
C) `$match`  
D) `$addFields` only

### Q40. Which aggregation finds maximum experience per role?
A) `$group:{ _id:"$role", maxExp:{ $max:"$experience" } }`  
B) `$group:{ role:{ $max:"$experience" } }`  
C) `$project:{ max:"$experience" }`  
D) `$match`

### Q41. Which aggregation finds minimum salary per location?
A) `$group:{ _id:"$location", minSalary:{ $min:"$salaryLPA" } }`  
B) `$sum`  
C) `$avg`  
D) `$project`

### Q42. Which stage reduces dataset size before grouping?
A) `$sort`  
B) `$match`  
C) `$limit`  
D) `$lookup`

### Q43. If index supports sort, blocking sort:
A) Happens  
B) Does not happen  
C) Always happens  
D) Never changes

### Q44. Which query finds jobs where role is Backend Developer AND experience between 2 and 5?
A) Implicit AND with `$gte` and `$lte`  
B) `$or`  
C) `$all`  
D) Not possible

### Q45. Which query finds jobs where salaryLPA is either 28, 30, or 35?
A) `$in`  
B) `$all`  
C) `$size`  
D) `$nor`

### Q46. Which aggregation counts total number of jobs?
A) `$count`  
B) `$group` with `$sum:1`  
C) Both A and B  
D) `$size`

### Q47. Which field in explain() shows returned documents?
A) `docsExamined`  
B) `nReturned`  
C) `totalDocs`  
D) `winningPlan`

### Q48. If index is not used, explain shows:
A) `IXSCAN`  
B) `COLLSCAN`  
C) `TEXTSCAN`  
D) `GROUP`

### Q49. Which query finds jobs where salaryLPA between 18 and 28 AND experience even?
A) `$gte,$lte` + `$mod`  
B) `$in`  
C) `$all`  
D) `$exists`

### Q50. Which query finds jobs where company starts with letter "G"?
A) `{ company:{ $regex:"^G" } }`  
B) `{ company:"G%" }`  
C) `{ company:{ $startsWith:"G" } }`  
D) `{ company:G* }`

### Q51. Which index improves filtering by company and experience together?
A) `{ company:1, experience:1 }`  
B) `{ experience:1 }`  
C) `{ company:-1 }`  
D) Text index

### Q52. Which stage flattens skills array before grouping?
A) `$unwind`  
B) `$split`  
C) `$flatten`  
D) `$explode`

### Q53. Which aggregation finds total salary per company?
A) `$group:{ _id:"$company", total:{ $sum:"$salaryLPA" } }`  
B) `$sum`  
C) `$project`  
D) `$match`

### Q54. Which query finds jobs where salaryLPA is greater than 40 OR experience ≥ 6?
A) `$or` query  
B) Implicit AND  
C) `$all`  
D) `$size`

### Q55. Which aggregation finds average experience per location?
A) `$group:{ _id:"$location", avgExp:{ $avg:"$experience" } }`  
B) `$sum`  
C) `$project`  
D) `$limit`

### Q56. If too many indexes exist, insert performance:
A) Improves  
B) Slows down  
C) Unaffected  
D) Doubles

### Q57. Which is best practice in aggregation performance?
A) Group first  
B) Match early  
C) Lookup first  
D) Sort first

### Q58. Which stage joins jobs with another collection?
A) `$join`  
B) `$lookup`  
C) `$merge`  
D) `$combine`

### Q59. Which query finds jobs where experience is exactly 5?
A) `{ experience:5 }`  
B) `{ experience:{ $eq:5 } }`  
C) Both A and B  
D) `{ experience ==5 }`

### Q60. Which is correct pipeline to get top 3 highest salary jobs?
A) `$limit` → `$sort`  
B) `$sort` → `$limit`  
C) `$group` → `$limit`  
D) `$match` → `$limit`

---

##  PART 2 – RESTAURANTS DATASET (Q61–Q80)

**Database:** `foodDB`  
**Collection:** `restaurants`

### Structure
```json
{
  "URL": "String",
  "address": "String",
  "address_line_2": "String",
  "name": "String",
  "outcode": "String",
  "postcode": "String",
  "rating": "Number",
  "type_of_food": "String"
}
```

### Q61. Which query displays only name and rating without _id?
A) `{ name:1, rating:1 }`  
B) `{ name:1, rating:1, _id:0 }`  
C) `{ name:true, rating:true }`  
D) `{ rating:0 }`

### Q62. Which query hides only the URL field but shows all other fields?
A) `{ URL:0 }`  
B) `{ URL:false }`  
C) `{ URL:null }`  
D) `{ URL:remove }`

### Q63. Which query sorts restaurants by rating from highest to lowest?
A) `.sort({ rating:1 })`  
B) `.sort({ rating:-1 })`  
C) `.order({ rating:-1 })`  
D) `.sort(rating)`

### Q64. Which query sorts restaurants alphabetically by name?
A) `.sort({ name:1 })`  
B) `.sort({ name:-1 })`  
C) `.sort(name)`  
D) `.order({ name:1 })`

### Q65. For page = 3 and page size = 10, what is the correct skip value?
A) 10  
B) 20  
C) 30  
D) 40

### Q66. Which query retrieves restaurants where rating is between 4 and 5 inclusive?
A) `{ rating:{ $gt:4, $lt:5 } }`  
B) `{ rating:{ $gte:4, $lte:5 } }`  
C) `{ rating:[4,5] }`  
D) `{ rating:{ $in:[4,5] } }`

### Q67. Which query finds restaurants where type_of_food is "Thai"?
A) `{ type_of_food:"Thai" }`  
B) `{ type_of_food:{ $eq:"Thai" } }`  
C) Both A and B  
D) `{ food:"Thai" }`

### Q68. Which query finds restaurants where name starts with letter "A"?
A) `{ name:{ $regex:"^A" } }`  
B) `{ name:"A%" }`  
C) `{ name:{ $startsWith:"A" } }`  
D) `{ name:A* }`

### Q69. Which query finds restaurants where postcode ends with "3JH"?
A) `{ postcode:{ $regex:"3JH$" } }`  
B) `{ postcode:"%3JH" }`  
C) `{ postcode:{ $end:"3JH" } }`  
D) `{ postcode:3JH$ }`

### Q70. Which query sorts first by type_of_food (A→Z) and then by rating (high→low)?
A) `.sort({ type_of_food:1, rating:-1 })`  
B) `.sort({ rating:-1, type_of_food:1 })`  
C) `.sort(type_of_food, rating)`  
D) `.order({ type_of_food:1, rating:-1 })`

### Q71. Which query returns the first 5 highest rated restaurants?
A) `.limit(5)`  
B) `.sort({ rating:-1 }).limit(5)`  
C) `.limit(5).sort({ rating:-1 })`  
D) `.sort({ rating:1 }).limit(5)`

### Q72. Which query returns the bottom 3 restaurants based on rating?
A) `.sort({ rating:-1 }).limit(3)`  
B) `.sort({ rating:1 }).limit(3)`  
C) `.limit(3)`  
D) `.skip(3)`

### Q73. Which query counts total number of restaurants?
A) `.count()`  
B) `.countDocuments()`  
C) Aggregation with `$count`  
D) All are valid approaches

### Q74. If you sort without an index on rating, explain() will likely show:
A) `IXSCAN`  
B) `COLLSCAN`  
C) `TEXTSCAN`  
D) `GROUP`

### Q75. Which index improves sorting by rating?
A) `{ rating:1 }`  
B) `{ name:1 }`  
C) `{ postcode:1 }`  
D) `{ type_of_food:1 }`

### Q76. Which query hides both address and address_line_2?
A) `{ address:0, address_line_2:0 }`  
B) `{ address:false, address_line_2:false }`  
C) Both A and B  
D) `{ address:null }`

### Q77. Which query retrieves restaurants where outcode starts with "CF"?
A) `{ outcode:{ $regex:"^CF" } }`  
B) `{ outcode:"CF%" }`  
C) `{ outcode:{ $start:"CF" } }`  
D) `{ outcode:CF* }`

### Q78. Why should sorting be applied before pagination?
A) To reduce memory  
B) To ensure correct page order  
C) To create index  
D) To group data

### Q79. If skip is applied before sort, result will:
A) Be correctly ordered globally  
B) Skip after sorting automatically  
C) Potentially return incorrect page ordering  
D) Cause error

### Q80. Which query returns page 2 of Thai restaurants (5 per page) sorted by rating descending?
A) `.find({type_of_food:"Thai"}).sort({rating:-1}).skip(5).limit(5)`  
B) `.find({type_of_food:"Thai"}).limit(5).skip(5).sort({rating:-1})`  
C) `.find().skip(5)`  
D) `.sort({rating:-1})`

---

##  PART 3 – USERS & ORDERS DATASET (Q81–Q100)

**Database:** `Flipkart`  
**Collections:** `users`, `orders`

### Structure
**users**
```json
{
  "_id": "Number",
  "name": "String",
  "city": "String",
  "membership": "Plus | Regular"
}
```

**orders**
```json
{
  "_id": "Number",
  "product": "String",
  "price": "Number",
  "userId": "Number"
}
```

### Q81. Which stage is used to join users and orders collections?
A) `$join`  
B) `$lookup`  
C) `$link`  
D) `$connect`

### Q82. In $lookup, which field connects users._id to orders.userId?
A) `foreignField`  
B) `localField`  
C) `joinField`  
D) `refField`

### Q83. Correct $lookup configuration to join users with orders is:
A)
```json
{
  "$lookup":{
    "from":"orders",
    "localField":"_id",
    "foreignField":"userId",
    "as":"orders"
  }
}
```
B)
```json
{
  "$lookup":{
    "from":"users",
    "localField":"userId",
    "foreignField":"_id",
    "as":"orders"
  }
}
```
C) `$join:"orders"`  
D) `{ $lookup:"orders" }`

### Q84. After $lookup, the joined data appears as:
A) Object  
B) String  
C) Array  
D) Number

### Q85. Which stage is required to flatten the orders array?
A) `$group`  
B) `$project`  
C) `$unwind`  
D) `$match`

### Q86. Which pipeline finds users from Delhi with their orders?
A)
```json
[
 { "$match":{ "city":"Delhi" } },
 { "$lookup":{ "from":"orders", "localField":"_id", "foreignField":"userId", "as":"orders" } }
]
```
B) Only `$lookup`  
C) Only `$match`  
D) `$group`

### Q87. To find users who have placed at least one order, we should:
A) `$match:{ orders:[] }`  
B) `$match:{ orders:{ $ne:[] } }`  
C) `$group`  
D) `$limit`

### Q88. To find users who have NOT placed any order:
A) `$match:{ orders:[] }`  
B) `$match:{ orders:{ $ne:[] } }`  
C) `$unwind`  
D) `$sort`

### Q89. Which stage calculates total spending per user?
A) `$project`  
B) `$group` with `$sum`  
C) `$match`  
D) `$lookup`

### Q90. Correct way to calculate total spending per user:
A)
```json
{
 "$group":{
   "_id":"$userId",
   "totalSpending":{ "$sum":"$price" }
 }
}
```
B) `{ $sum:"price" }`  
C) `{ $group:"price" }`  
D) `{ total:{ $sum } }`

### Q91. Which stage must come before grouping if we want city-wise revenue?
A) `$lookup`  
B) `$match`  
C) `$unwind`  
D) `$sort`

### Q92. Which accumulator calculates average order value?
A) `$sum`  
B) `$avg`  
C) `$count`  
D) `$min`

### Q93. Which accumulator finds highest priced order?
A) `$min`  
B) `$avg`  
C) `$max`  
D) `$sum`

### Q94. To rename price as orderPrice, which stage is used?
A) `$group`  
B) `$match`  
C) `$project`  
D) `$lookup`

### Q95. Which pipeline correctly sorts users by total spending descending?
A)
```json
[
 { "$lookup":{...} },
 { "$unwind":"$orders" },
 { "$group":{ "_id":"$_id", "total":{ "$sum":"$orders.price" } } },
 { "$sort":{ "total":-1 } }
]
```
B) `$sort` only  
C) `$match` only  
D) `$limit`

### Q96. If $lookup is used without $unwind, grouping by price will:
A) Fail  
B) Work on arrays  
C) Automatically flatten  
D) Remove documents

### Q97. Which stage reduces fields in final output?
A) `$match`  
B) `$project`  
C) `$group`  
D) `$lookup`

### Q98. Which pipeline order is correct?
A) `$group` → `$match` → `$lookup`  
B) `$match` → `$lookup` → `$group`  
C) `$sort` → `$match` → `$lookup`  
D) `$limit` → `$group` → `$lookup`

### Q99. Which query finds top 3 expensive orders with user details?
A)
```json
[
 { "$lookup":{...} },
 { "$unwind":"$orders" },
 { "$sort":{ "orders.price":-1 } },
 { "$limit":3 }
]
```
B) `$limit` first  
C) `$group` only  
D) `$match` only

### Q100. Which statement is TRUE about $lookup?
A) It performs inner join only  
B) It performs left outer join  
C) It removes unmatched documents  
D) It modifies data permanently

---
---

#  SECTION B – Practical Queries (Q101–Q150)
*Write the exact MongoDB Query/Aggregation Pipeline for the following questions.*

##  PART 1 – JOBS DATASET (Q101–Q120)

**Database:** `codinggita`  
**Collection:** `jobs`

### Q101. Write a query to find all jobs where salaryLPA is greater than 25.
### Q102. Find jobs where experience is between 2 and 5 years (inclusive).
### Q103. Find jobs where company is Google or Amazon.
### Q104. Find jobs where location is not Bangalore.
### Q105. Find jobs where experience is an even number.
### Q106. Find jobs where salaryLPA is greater than experience multiplied by 5.
### Q107. Find jobs where experience is less than 3 and salaryLPA is greater than 20.
### Q108. Find jobs where skills include "MongoDB".
### Q109. Find jobs where skills include both "Node.js" and "MongoDB".
### Q110. Find jobs where role starts with "Backend".
### Q111. Display only company, role, and salaryLPA (hide _id).
### Q112. Display all fields except skills.
### Q113. Sort jobs by salaryLPA from highest to lowest.
### Q114. Sort jobs by experience ascending and salaryLPA descending.
### Q115. Display top 5 highest salary jobs.
### Q116. Display page 3 assuming 10 jobs per page.
### Q117. Find jobs located in Bangalore sorted by salaryLPA descending and return only first 3 results.
### Q118. Create an index to optimize searching by location and verify using explain().
### Q119. Create a compound index to optimize filtering by location and sorting by salaryLPA.
### Q120. Write an aggregation query to find average salary per location.

---

##  PART 2 – AGGREGATION ON JOBS (Q121–Q130)
*Use only: `$match`, `$project`, `$group`*

### Q121. Find total number of jobs per company.
### Q122. Find maximum salary per location.
### Q123. Find minimum experience per company.
### Q124. Find average salary per jobType.
### Q125. Find total salary paid per company.
### Q126. Find total number of jobs where experience ≥ 3 grouped by location.
### Q127. Find average salary per role where location is Bangalore.
### Q128. Rename salaryLPA to salary using $project.
### Q129. Show company-wise total salary and hide _id in final output.
### Q130. Full pipeline: Filter Full-time jobs → group by location → find average salary.

---

## 🔹 PART 3 – RESTAURANTS DATASET (Q131–Q140)

**Database:** `foodDB`  
**Collection:** `restaurants`

### Q131. Display only name and rating without _id.
### Q132. Hide URL field from output.
### Q133. Find restaurants where rating is between 4 and 5.
### Q134. Find restaurants where name starts with letter "A".
### Q135. Sort restaurants by rating descending.
### Q136. Sort by type_of_food ascending and rating descending.
### Q137. Display first 10 restaurants.
### Q138. Display second page assuming 15 per page.
### Q139. Display top 5 highest rated Thai restaurants.
### Q140. Count total number of restaurants with rating = 5.

---

## 🔹 PART 4 – USERS & ORDERS ($lookup) (Q141–Q150)

**Database:** `Flipkart`  
**Collections:** `users`, `orders`

### Q141. Write an aggregation query using $lookup to display all users with their orders.
### Q142. Display only name, city, and orders (hide membership and _id).
### Q143. Find users who have placed at least one order.
### Q144. Find users who have not placed any order.
### Q145. Calculate total spending of each user.
### Q146. Find highest priced order for each user.
### Q147. Find total revenue generated per city.
### Q148. Display top 3 most expensive orders with user name.
### Q149. Find total number of orders placed by each membership type.
### Q150. (Intentionally left blank/missing question instructions in source)
