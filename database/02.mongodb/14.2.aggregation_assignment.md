# MongoDB Aggregation Assignment

> [!IMPORTANT]
> **Mandatory Instructions (Must Be Followed Strictly)**
> These instructions are mandatory and are designed to reflect real backend / data-engineering practices.
> All learners are expected to follow them strictly.

###  Data Source Rules

*   All questions must be solved using the provided **`jobs.json`** dataset only.
*   Creating new data is **strictly prohibited**.
*   Modifying existing documents is **NOT allowed**.
*   Inserting additional records into the collection is **NOT permitted**.

###  Database Configuration

*   **Database Name:** `codinggita`
*   **Collection Name:** `jobs`
*   **Data File:** `jobs.json`

> [!WARNING]
> **🚫 Usage Restrictions**
> *   ❌ `find()` is **NOT allowed**.
> *   ❌ Update operations are **NOT allowed**.
> *   ❌ Delete operations are **NOT allowed**.
> *   ❌ Any aggregation stage other than allowed stages is **NOT permitted**.
> *   ✅ Only **`aggregate()`** must be used.
> *   ✅ **Allowed stages only:**
>     *   `$match`
>     *   `$project`
>     *   `$group`
> *   ✅ **Allowed operators only:**
>     *   `$sum`
>     *   `$avg`
>     *   `$min`
>     *   `$max`
> *   Students must use only the MongoDB aggregation concepts covered in class.

###  Execution Rules (Very Important)

1.  Every aggregation query must first be written and executed in **Mongo Shell (`mongosh`)**.
2.  The output of each query must be **carefully verified**.
3.  Result must correctly satisfy the question’s requirement.
4.  If output is incorrect, the query must be corrected and re-tested.

###  Submission Process

1.  Only **verified aggregation queries** should be written on paper.
2.  ❌ Writing queries directly on paper **without testing** is NOT acceptable.
3.  ❌ Guess-based answers are **strictly prohibited**.
4.  Queries written on paper must **exactly match** the verified Mongo Shell queries.

---

##  Assignment Questions

###  LEVEL 1 – WARM-UP (Aggregation Mindset)
**(Q1–Q20)**

1.  Write an aggregation query to find all jobs located in Bangalore.
2.  Write an aggregation query to find all jobs where experience is exactly 3.
3.  Write an aggregation query to find all jobs where salary is more than 30 LPA.
4.  Write an aggregation query to find all Remote jobs.
5.  Write an aggregation query to find all jobs where role is Backend Developer.
6.  Write an aggregation query to find all jobs where company is Google or Amazon.
7.  Write an aggregation query to find all jobs where experience is less than 2.
8.  Write an aggregation query to find all jobs where salary is between 15 and 25 LPA.
9.  Write an aggregation query to find all Contract jobs.
10. Write an aggregation query to find all Intern jobs.
11. Write an aggregation query to show only company and role for all jobs.
12. Write an aggregation query to show only company and salaryLPA and hide _id.
13. Write an aggregation query to show only location and jobType.
14. Write an aggregation query to rename salaryLPA to salary for all jobs.
15. Write an aggregation query to show companyName instead of company.
16. Write an aggregation query to show only company and experience.
17. Write an aggregation query to show only role and location.
18. Write an aggregation query to show only company, salaryLPA, and jobType.
19. Write an aggregation query to show only company and hide all other fields.
20. Write an aggregation query to show only company and skills.

---

###  LEVEL 2 – REPORT BASICS
**(Q21–Q40)**

21. Write an aggregation query to find the total number of jobs in the database.
22. Write an aggregation query to find how many jobs exist in each location.
23. Write an aggregation query to find how many jobs exist in each jobType.
24. Write an aggregation query to find how many jobs exist for each company.
25. Write an aggregation query to find how many jobs exist for each role.
26. Write an aggregation query to find the average salary per location.
27. Write an aggregation query to find the average salary per jobType.
28. Write an aggregation query to find the average salary per company.
29. Write an aggregation query to find the average experience per role.
30. Write an aggregation query to find the average experience per location.
31. Write an aggregation query to find the minimum salary per location.
32. Write an aggregation query to find the maximum salary per location.
33. Write an aggregation query to find the minimum experience per company.
34. Write an aggregation query to find the maximum experience per company.
35. Write an aggregation query to find the minimum salary per jobType.
36. Write an aggregation query to find the maximum salary per jobType.
37. Write an aggregation query to find the total salary paid per location.
38. Write an aggregation query to find the total salary paid per company.
39. Write an aggregation query to find the total salary paid per jobType.
40. Write an aggregation query to find the total jobs per experience value.

---

###  LEVEL 3 – FILTER + REPORT (Basic)
**(Q41–Q65)**

41. Write an aggregation query to find total jobs per location where experience ≥ 3.
42. Write an aggregation query to find average salary per location where jobType is Full-time.
43. Write an aggregation query to find total jobs per company where location is Bangalore.
44. Write an aggregation query to find average salary per role where experience ≥ 4.
45. Write an aggregation query to find minimum salary per location where jobType is Contract.
46. Write an aggregation query to find maximum salary per location where jobType is Full-time.
47. Write an aggregation query to find total jobs per role where salary ≥ 25.
48. Write an aggregation query to find average salary per company where location is Remote.
49. Write an aggregation query to find minimum salary per company where experience ≥ 5.
50. Write an aggregation query to find maximum salary per company where experience ≥ 5.
51. Write an aggregation query to find total salary per location where jobType is Full-time.
52. Write an aggregation query to find average salary per location where experience is exactly 2.
53. Write an aggregation query to find total jobs per company where salary ≤ 15.
54. Write an aggregation query to find minimum salary per role where location is Bangalore.
55. Write an aggregation query to find maximum salary per role where location is Remote.
56. Write an aggregation query to find average experience per jobType where salary ≥ 20.
57. Write an aggregation query to find total jobs per jobType where experience ≤ 2.
58. Write an aggregation query to find total salary per company where jobType is Full-time.
59. Write an aggregation query to find average salary per role where company is Google.
60. Write an aggregation query to find total jobs per location where company is Amazon.
61. Write an aggregation query to find average salary per company where jobType is Contract.
62. Write an aggregation query to find minimum experience per role where salary ≥ 25.
63. Write an aggregation query to find maximum experience per role where location is Bangalore.
64. Write an aggregation query to find total salary per location where experience ≥ 3.
65. Write an aggregation query to find average salary per jobType where location is Remote.

---

###  LEVEL 4 – FULL PIPELINE (MANDATORY ALL 3 STAGES)
**(Q66–Q100)**
`$match` → `$project` → `$group`

66. Write an aggregation query to filter Bangalore jobs, then show location-wise total jobs with clean output.
67. Write an aggregation query to filter Full-time jobs, then show company-wise average salary, hide _id.
68. Write an aggregation query to filter Remote jobs, then show role-wise total jobs, rename output fields.
69. Write an aggregation query to filter jobs with experience ≥ 3, then show location-wise minimum salary.
70. Write an aggregation query to filter jobs with salary ≥ 25, then show company-wise maximum salary.
71. Write an aggregation query to filter Contract jobs, then show location-wise average salary.
72. Write an aggregation query to filter Intern jobs, then show company-wise total jobs.
73. Write an aggregation query to filter Bangalore jobs, then show jobType-wise total salary.
74. Write an aggregation query to filter Remote jobs, then show company-wise average experience.
75. Write an aggregation query to filter jobs with experience ≤ 2, then show location-wise total jobs.
76. Write an aggregation query to filter jobs with salary ≤ 15, then show company-wise average salary.
77. Write an aggregation query to filter Full-time Bangalore jobs, then show role-wise total jobs.
78. Write an aggregation query to filter Remote Full-time jobs, then show location-wise average salary.
79. Write an aggregation query to filter jobs with experience ≥ 5, then show company-wise minimum salary.
80. Write an aggregation query to filter jobs with salary ≥ 30, then show role-wise maximum salary.
81. Write an aggregation query to filter Bangalore jobs, then show company-wise total salary.
82. Write an aggregation query to filter Remote jobs, then show jobType-wise total jobs.
83. Write an aggregation query to filter Full-time jobs, then show location-wise average salary.
84. Write an aggregation query to filter Contract jobs, then show company-wise minimum experience.
85. Write an aggregation query to filter Intern jobs, then show location-wise total jobs.
86. Write an aggregation query to filter jobs with experience ≥ 3, then show jobType-wise average salary.
87. Write an aggregation query to filter jobs with salary ≥ 20, then show location-wise total salary.
88. Write an aggregation query to filter Remote jobs, then show company-wise maximum salary.
89. Write an aggregation query to filter Bangalore jobs, then show role-wise average salary.
90. Write an aggregation query to filter jobs with experience ≤ 2, then show company-wise total jobs.
91. Write an aggregation query to filter Full-time jobs, then show role-wise minimum salary.
92. Write an aggregation query to filter Contract jobs, then show location-wise maximum salary.
93. Write an aggregation query to filter Remote jobs, then show company-wise total salary.
94. Write an aggregation query to filter jobs with salary ≥ 25, then show jobType-wise total jobs.
95. Write an aggregation query to filter Bangalore jobs, then show company-wise average experience.
96. Write an aggregation query to filter jobs with experience ≥ 4, then show location-wise total jobs.
97. Write an aggregation query to filter jobs with salary ≤ 20, then show role-wise average salary.
98. Write an aggregation query to filter Full-time jobs, then show company-wise total salary.
99. Write an aggregation query to filter Remote jobs, then show location-wise minimum salary.
100. Write an aggregation query to filter jobs with experience ≥ 3, then show a company-wise complete salary report
